# Chapter 3 多线程服务器的使用与常用编程模型
```
1.线程非常的廉价(note:这里的线程不是OS线程是语言层面上自行调度的线程)
python gevent 协成 GO goroutime erlang actor 

2.OS 线程
    libevent muduo Netty


可以模式
1.运行一个单线程的进程
2.运行一个多线程的进程
3.运行多个单线程的进程
    目前公认为的主流模式
        1.把模式1复制几份
        2.主进程+worker进程 必须绑定到一个tcp port (httpd+fastcgi 就是这种模式)

4.运行多个多现成的进程


模式2 和模式3.2 的优劣势


单线程场合
1.程序可能会fork(2)
    多线程 fork(2) 会遇到特别多的麻烦 不建议多线程fork(2)



fork(2) 应用场景
    1.fork(2) 立即 exec() 例如 shell 等程序
    2.fork(2) 继续当前程序执行 为了共享文件描述符合和父进程通信 协同完成任务 or 接过父进程的任务继续完成


一些辅助性的程序 例如监控其他程序的状态 那么这些程序做成单线程可以不变过分抢夺系统的计算资源

    例如 如果我们要将生产服务器的日志压缩放在NFS上 
        压缩程序选用单线程程序 如果服务器8core 即使压缩打满整核 也只用了计算资源的 12.5%

        如果我们选用了多线程的压缩程序 如果计算资源占用过多 就会影响用于请求服务器的响应速度

 2.限制CPU的占用率


单线程程序的优缺点:
    单线程 io 多路复用程序

    事件a 1ms 做完  事件b 10ms做完


    b 的事件优先于 a 触发 a 做完这件事情 要11ms 之后,响应时间需要11ms


    解决改问题 使用多线程来克服


多线程程序有性能优势吗

    1.例如静态服务器 或者FTP 服务器 性能的瓶颈在磁盘IO 和网络IO
      单线程程序可能就能到达瓶颈,如果使用多线程并不能提高吞吐量

    多线程虽然能提高CPU 的利用率 但是有的时候程序过于复杂 用3.1 多个单线程的进程的方式可能程序更加简单


1.
Linux 能启动多少个线程

32 bit linux

一个进程的地址空间 4GB 用户太能访问 差不3GB 左右的空间
一个线程栈的大小是10MB(默认) 根据计算一个32bits linux 可以启动差不多300个线程

对于64bits linux 操作系统数目大大增加


2.多线程能提高并发度吗
   单个 evenloop 处理 并发1万连接并不罕见 一个 multi-loop的独显程序轻松支持5万个并发连接


    threa per connection 并不适合高并发场景 ,(scalability(可扩展)不佳) one loop thread 的并发度足够大 且与cup数目成正比



3.多线程能提高吞吐量吗
  计算密集性服务 不能
   
   如果单线程 执行完一个任务 需要 0.8s 那个一个线程的吞量 为 1.25 qps(1/0.8)

   但是该服务器是 8 core ,理论上可以达到 10qps(显示中可能会打8折)



    thread per request 模型去处理 客户强求 动并发数很大的时候 吞吐量会降序 是因为 线程过多每次线程上下文切换开销也增加的非常多




Proactor 模型 ：(异步)
    I O操作读写 全部由OS 内核代理执行 ,当执行完毕告诉你已经完成的时间通知你,你只需用直接处理缓冲里面的数据

    代表:windows iocp ,linux  io_uring, boost .asio 

Reactor 模型;(同步非阻塞)
    引用层自己调用 read write
    当可以读写的时候 OS 告诉引用层 应用层自己学些数据到缓冲区 or 应用层


     代表 epoll(linux)  select ,Java NIO 



```

