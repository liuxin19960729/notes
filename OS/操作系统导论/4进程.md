# 4进程
## 4.5 数据结果
```
进程停止时OS会讲进程相关的上下文信息保存做一快照(用于后面恢复进程运行能够从停止的地方继续执行)
保存的上下文
    1.寄存器值和信息


进程状态
1.运行
2.就绪
3.阻塞
4.创建阶段(initial)
5.退出未清理(final) 在UNIX 中这种状态的进程被称为僵尸进程
    note:这个状态非常有用程序 用于其他进程检查该进程是否成功执行
    通常返回0 表示成功执行
    父进程调用wait() 之后 返回 告诉 OS 可以清理该子进程的数据结构
```